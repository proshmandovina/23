#Задание 2
def f(start, end):
    if start > end or start == 50:
        return 0
    if start == end:
        return 1
    if start < end:
        return f(start + 2, end) + f(start * 2, end)

print(f(10, 100))
